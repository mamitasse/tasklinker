{{ form_start(form) }}

    {{ form_row(form.name) }}

    {{ form_row(form.users) }}

    <br>
    <button class="button button-submit">
        {{ button_label|default('Continuer') }}
    </button>

{{ form_end(form) }}
